## 2024-05-23 - Icon-Only Buttons Accessibility
**Learning:** Icon-only buttons (like calendar navigation arrows) are completely invisible to screen readers without `aria-label`.
**Action:** Always add `aria-label` to buttons that only contain an icon.
